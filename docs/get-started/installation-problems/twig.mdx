- Add the `--type twig` flag to the installation command to set up Storybook manually:

  ```shell
  npx sb init --type twig
  ```

- For other installation issues, check the [Twig README](../../app/twig/README.md) for additional instructions.